#+TITLE: Walkthrough Shared Conventions
#+WALKTHROUGH_GUIDE_VERSION: 25

Single source of truth for conventions shared by
[[file:01-index-guide.org][01-index-guide.org]] (the index),
[[file:02-expansion-guide.org][02-expansion-guide.org]] (walkthrough
packages), [[file:04-archive-guide.org][04-archive-guide.org]]
(archiving a settled package),
[[file:07-code-suggestions-guide.org][07-code-suggestions-guide.org]]
(suggesting code inside a =[you-run]= step without writing it into the repo),
[[file:08-context-guide.org][08-context-guide.org]] (the research
staging area at =.agents/walkthroughs/CONTEXT/=), and
[[file:10-ingestion-guide.org][10-ingestion-guide.org]] (ingesting your
own already-researched material via =.agents/walkthroughs/inbox/=, a
deliberately separate pathway from agent-sourced CONTEXT research). All
six reference this file instead of restating it — a rule here changes
in exactly one place, not several that can drift apart. Checked
mechanically by =SCRIPTS/validate-walkthroughs.py= (walkthroughs and
index).

See also [[file:09-workflow-guide.org][09-workflow-guide.org]] for a
cross-file map of how these guides' lifecycles (a CONTEXT file, an index
issue, a walkthrough package) interact — a router that points at the
rules below, never restates them, so it isn't part of the "changes in
exactly one place" chain above and doesn't gate this file's own version
number.

* Git Usage

*CRITICAL RULE — read-only git commands only.* Every guide in this system
assumes the agent can freely inspect the repository's history and
working-tree state to verify claims independently instead of trusting
them — =git status=, =git diff= (working tree, staged, or between any two
commits/branches), =git log=, =git show=, =git blame=, and similar. See
[[file:02-expansion-guide.org][02-expansion-guide.org]]'s External Session
Workflow for exactly this use, checking a session export's claims against
the actual state of the repo.

The agent must never use git to *mutate* anything, though — no staging
(=git add=), no committing, no pushing, no rewriting history (=git reset=,
=git rebase=, =git filter-branch=, anything with =--force=/=-f=), no
discarding working-tree or staged changes (=git checkout --=, =git
restore=, =git clean=, =git stash pop=/=apply=/=drop=), no deleting
branches or tags, no touching remotes or git config. This holds
regardless of what a session export claims was already committed, how
confident the agent is that a change is correct, or how convenient
committing would be right now — the repository's history is the human's
own record of their own work, same reason a =[you-run]= step's actual
implementation stays theirs to type (see
[[file:07-code-suggestions-guide.org][07-code-suggestions-guide.org]]).
Requested directly, immediately after =git status=/=git diff= were
identified as a genuinely better way to verify what changed during a
session than re-reading individual files.

* UUID Convention

Every linkable node in this system — an index issue, a walkthrough
step — gets a permanent =:ID:= via a =:PROPERTIES:= drawer:

#+begin_src org
:PROPERTIES:
:ID:       <uuid>
:END:
#+end_src

- *CRITICAL RULE — generate, never invent.* Produce the UUID with a
  real generator: =uuidgen=, or =python3 -c 'import uuid;
  print(uuid.uuid4())'= if that's unavailable. Never hand-type a
  UUID-shaped string. An LLM-typed "plausible" UUID isn't actually
  random and risks colliding with one already in use.
- *CRITICAL RULE — never in visible text.* The UUID lives strictly
  inside the drawer — never in a heading, never in a checklist item's
  own text. Headings and step descriptions stay clean and
  human-readable.
- *CRITICAL RULE — one namespace.* Every UUID under
  =.agents/walkthroughs/= — index nodes, package directory names,
  individual walkthrough steps, and CONTEXT file entries (see
  [[file:08-context-guide.org][08-context-guide.org]]'s Naming &
  Lifecycle) — must be globally unique, not just within one file. Run
  =SCRIPTS/validate-walkthroughs.py= to check.

* Issue-Tracker Mental Model

Every Level 3 node in [[file:../index.org][index.org]] is an *issue*:
title (heading), body (short description), state (=TODO=/=DONE= via
Org's native keyword — open vs. closed), optional tags (Org's
=:tag:= syntax, standing in for labels), and id (the =:ID:= UUID
above, standing in for an issue number). Closing an issue means
expanding it into a walkthrough package — see
[[file:02-expansion-guide.org][02-expansion-guide.org]]. There's no
"resolved but undocumented" state: closed implies the write-up exists.

* Guide Version

=#+WALKTHROUGH_GUIDE_VERSION:= at the top of this file is the current
version of the required =walkthrough.org= structure. Every
=walkthrough.org= must carry a matching
=#+WALKTHROUGH_GUIDE_VERSION:= line directly under its =#+DATE:=.

*This version number gates =walkthrough.org='s own required structure
only.*

Bump the number here whenever the required structure actually changes
— a section added, removed, or renamed, or a new CRITICAL RULE that
changes what "valid" means. Existing walkthrough packages are *not*
required to be retrofitted immediately when the version bumps — that
would make every structural change a repo-wide scramble. Instead, a
walkthrough's version number falling behind current is exactly the
signal that it may now be missing something the guide requires;
=SCRIPTS/validate-walkthroughs.py= reports this as a warning, not a
failure, so staleness stays visible instead of silent.

** Changelog

- *v25* (2026-09-18): Removed the wiki module
  ([[file:05-wiki-guide.org][05-wiki-guide.org]], =.agents/wikis/=)
  entirely, along with every cross-reference to it. Requested directly,
  after an audit found =.agents/wikis/= had never actually been
  instantiated — not sparse, never created at all — despite 8 archived
  walkthroughs each landing on a real "current state of the project"
  decision that the guide's own trigger rule said should have produced a
  page. Each archived walkthrough's own final =*Result:*= block had
  already been serving as the de facto current-state reference in
  practice, so the wiki's actual value proposition never materialized as
  real pain at this project's scale. =SCRIPTS/validate-walkthroughs.py='s
  =validate_wikis()= function, =WIKIS_DIR=/=WIKI_META_FILES= constants,
  and the call to it in =main()= are all removed; every other guide's
  cross-reference to the wiki (=07-code-suggestions-guide.org=,
  =04-archive-guide.org=, =02-expansion-guide.org='s concept-promotion
  flag and Notes template, =08-context-guide.org='s "CONTEXT is not the
  wiki" framing, =09-workflow-guide.org='s Quick Reference row) is
  removed or rewritten rather than left dangling. The two other unused
  features found in the same audit — =02-expansion-guide.org='s
  Step-Level Expansion, and =08-context-guide.org='s direct-paste
  =SOURCE: you= pathway — were deliberately *not* removed: unlike the
  wiki, both cost nothing while dormant (no standing directory structure,
  no validator checks, opt-in prose only), so cutting them would be
  churn without a matching benefit. Old changelog entries below that
  mention the wiki (v8, v9, v18, and others) are left exactly as
  written, same as v13 left pre-existing Quiz/Flashcards mentions
  untouched — this changelog is a historical record, not rewritten to
  match the system's current shape.
- *v24* (2026-09-18): New file
  [[file:10-ingestion-guide.org][10-ingestion-guide.org]] and
  =.agents/walkthroughs/inbox/=: a deliberately separate pathway from
  agent-sourced CONTEXT research, modeled on an ingestion workflow from a
  different personal-knowledge-management agent the human uses, adapted
  rather than adopted wholesale. The source workflow's core trigger model
  — continuous, implicit, permission-free background writing — was
  explicitly rejected as incompatible with this system's existing
  approval-gated design (CONTEXT's "no filing without your go-ahead,"
  the wiki's deliberate-update-only model, and the general "nothing gets
  written without an explicit, attributable trigger" principle every
  other guide in this system depends on). What *did* transfer: a staging
  directory for material the human already researched themselves (text,
  PDFs, photos, office documents), ingested one file at a time on
  explicit request, each producing a filed =index.org= issue (new, if
  none existing fits) and =CONTEXT/<issue-uuid>/= entries with a new
  =:SOURCE_FILE:= provenance property, a one-line receipt printed before
  deletion, and the file deleted only once actually, successfully
  ingested — never guessed at, never batch-deleted on the strength of one
  successful file in the same pass.
  =SCRIPTS/validate-walkthroughs.py='s =SKIP_DIRS= gained =inbox= so it's
  recognized as a legitimate, contents-transient directory rather than an
  orphan. =08-context-guide.org='s own Workflow: User-Sourced Material
  section was updated to describe both hand-off forms (direct paste vs.
  inbox file) and point to the new guide for the latter, rather than
  restating it.
- *v23* (2026-09-15): New section in
  [[file:07-code-suggestions-guide.org][07-code-suggestions-guide.org]]
  ("When the Human Explicitly Asks for a Full Example") and a matching
  bullet added to [[file:02-expansion-guide.org][02-expansion-guide.org]]'s
  Template =For AI Assistants= boilerplate: the =*Suggested starting
  point*= partial-example framing is for code offered proactively, before
  the human has tried — once the human has actually tried, gotten stuck,
  and explicitly asks for a complete example, give the whole thing
  directly, without another partial hint or a repeated "type it yourself"
  disclaimer stacked on a request already made plainly. Does *not* loosen
  the unconditional rule that the agent never uses a file-writing tool on
  the real project file — only what gets *said*, not who types the file.
  Requested directly after exactly this stacking happened in a real
  session: a human explicitly said "I cant think of a way to implement it
  and i want to learn," got a correct example wrapped in enough
  "verify this yourself" language that it read as gatekeeping. Also
  updated the then-active =2e517fe3= walkthrough's own =For AI
  Assistants= bullet to match, since it's a live document an external AI
  session might read right now, and added a matching row to
  [[file:09-workflow-guide.org][09-workflow-guide.org]]'s phrasing table.
- *v22* (2026-09-14): =.agents/walkthroughs/CONTEXT/= restructured from a
  single flat directory (one shared =context_index.org= tracking every
  research file from every issue) into one subdirectory per owning issue
  (=CONTEXT/<issue-uuid>/=), each with its own =index.org= — mirroring
  the existing one-directory-per-issue shape walkthrough packages already
  use. =SCRIPTS/context-status.py=, =SCRIPTS/context-mine.py=, and
  =SCRIPTS/validate-walkthroughs.py='s =validate_context()= all rewritten
  to walk every subdirectory instead of assuming one fixed file;
  =validate_context()= also gained a new check mirroring the existing
  walkthrough-package validation — each subdirectory's name must be a
  valid UUID matching some real =index.org= node (any state, since
  CONTEXT research routinely predates an issue's own expansion, unlike a
  walkthrough package which does require =DONE=). All 17 existing
  research files and their 19 citing =[[file:...]]= links across four
  walkthroughs (one archived per issue, plus the then-active =2e517fe3=
  package) were migrated and rewritten to match. Requested directly,
  after the flat directory's growing file count (17 files across four
  issues, one shared index) made it hard to tell at a glance which
  research belonged to which issue.
- *v21* (2026-09-13): Two additions, requested together in one message.
  (1) New =* Git Usage= section (above), formalizing a CRITICAL RULE that
  already applied in spirit but had never been written down: read-only
  git commands (=status=/=diff=/=log=/=show=/=blame=) are always fine for
  independent verification, but the agent must never stage, commit, push,
  or otherwise mutate the repository's history or working tree via git,
  no matter what a session export claims or how convenient it would be.
  (2) New CRITICAL RULE in
  [[file:02-expansion-guide.org][02-expansion-guide.org]]'s External
  Session Workflow — check the full =git status=/=git diff= before
  reconciling a session export, not just the files it happens to mention,
  since an export can under-report real work just as easily as it can
  over-claim. Both requested directly, immediately after =git status=
  surfaced real, already-started implementation work (new projects, moved
  and deleted files) that no session export had reported yet — proving
  the gap in real time, not hypothetically.
- *v20* (2026-09-13): New =SCRIPTS/context-mine.py=, documented in
  [[file:08-context-guide.org][08-context-guide.org]]'s new "Mining a
  File" section — automates only the bookkeeping half of mining a CONTEXT
  file (flip =:STATUS:= to =mined=, re-run the validator), and refuses to
  run at all unless the citing file already contains a real
  =[[file:...]]= link to that CONTEXT uuid. Deciding what the research
  justifies adding, and writing it, stays a human/agent judgment call the
  script never touches — same split as every other tool in this system
  (=validate-walkthroughs.py= checks structure, never content;
  =context-status.py= reports status, never decides it). Requested
  directly, to avoid repeating the same multi-step manual bookkeeping
  every time a CONTEXT file gets mined.
- *v19* (2026-09-13): Fixed a real inconsistency in
  [[file:08-context-guide.org][08-context-guide.org]]'s own template,
  found directly by the human reading it: a =context_index.org= entry's
  heading was the raw UUID itself (=* <uuid>=), which flatly contradicts
  this file's own pre-existing "never in visible text" CRITICAL RULE under
  UUID Convention above — a rule that already applied to every linkable
  node in this system before =08-context-guide.org= was ever written. The
  template now uses a short, semantic title for the heading, with the
  =:ID:= staying in the drawer exactly like everywhere else.
  =SCRIPTS/validate-walkthroughs.py='s =validate_context()= gained a
  matching mechanical check (reusing the same =UUID_ANY_RE= check
  =validate_index()= already applies to index.org headings), and the one
  existing =context_index.org= entry was migrated to a real title. Not a
  new rule — a fix to bring a brand-new file into compliance with a rule
  that predates it.
- *v18* (2026-09-13): New file
  [[file:08-context-guide.org][08-context-guide.org]] and
  =.agents/walkthroughs/CONTEXT/=: a staging area for raw research material
  on its way to becoming a filed =index.org= issue, distinct from the wiki
  (settled knowledge) and from a walkthrough package (a solved problem's
  write-up). Two trust levels, not conflated: material the human drops in
  themselves (=SOURCE: you=) is auto-approved to mine directly, since
  choosing to put it there already is the approval; material the agent goes
  and finds on its own initiative (=SOURCE: agent=, modeled on Google
  NotebookLM's Discover-Sources pattern) starts =STATUS: proposed= and a new
  CRITICAL RULE — no filing without your go-ahead — blocks any issue from
  citing it until the human says which parts actually apply. Metadata
  (=:ID:=/=:SOURCE:=/=:STATUS:=) lives out-of-band in a sibling
  =context_index.org=, never written into the raw file itself, so dropped-in
  material stays byte-for-byte what was actually written or found — same
  instinct as a SESH export never being converted to =.org= (v6, below).
  =SCRIPTS/validate-walkthroughs.py= gained a matching =validate_context()=
  check (valid =:ID:=/=:SOURCE:=/=:STATUS:=, a resolving =file:= link, no
  orphaned CONTEXT file missing its entry), and a companion
  =SCRIPTS/context-status.py= reports what's still =proposed= or =approved=
  but not yet =mined=. This version bump and changelog entry were
  specifically missing when the system was first introduced — added now as
  routine self-consistency maintenance, the same discipline this file asks
  of every other structural change.
- *v17* (2026-09-12): New CRITICAL RULE in
  [[file:04-archive-guide.org][04-archive-guide.org]]'s Archive Workflow
  (new step 8): when archiving a package, also grep every *other*
  already-archived file for links pointing at this package's pre-archive
  path (=<uuid>/walkthrough.org=), and update just the path to
  =archive/<uuid>.org=. This isn't the "never rewritten" rule being
  violated — a link's address following where its target moved is the
  same kind of update step 7 already makes to =index.org=; the archived
  file's own prose/decisions/=*Result:*= blocks stay untouched. Found by
  hitting this exact case directly: archiving the seed-data tool
  walkthrough broke a citation inside the already-archived =.NET SDK=
  walkthrough, caught immediately as a hard failure by
  =validate-walkthroughs.py='s existing unconditional link-integrity
  check — no validator change needed, the check already covered archived
  files, this was a gap in the *workflow* that hadn't hit this ordering
  before.
- *v16* (2026-09-11): New file
  [[file:07-code-suggestions-guide.org][07-code-suggestions-guide.org]] and a
  matching CRITICAL RULE it defines: a =[you-run]= step whose lesson is
  writing implementation code must include an inline, clearly-labeled
  "Suggested starting point" code block in =walkthrough.org= itself, but the
  agent authoring or working the walkthrough must never use Write/Edit to put
  that code into the actual project file (=Program.cs=, =docker-compose.yml=,
  etc.) — that stays the human's own typing regardless of how confident the
  agent is in the answer. Requested directly, across a real sequence in one
  project's walkthroughs: the agent first wrote a complete C# console tool
  straight into the repo unprompted (immediately reverted — "Do not write the
  tool for me, i want to learn too"), then a docker-compose walkthrough was
  authored with zero inline code at all to avoid repeating that mistake, then
  the human clarified the actual boundary was narrower than that overcorrection
  — "You are able to suggest code, just dont write it into the repo, thats my
  job" — and asked for the suggestion to live in the walkthrough file, not just
  chat, before generalizing it to every future walkthrough. Distinct from a
  =*Result:*= block (reserved for real, verified output once a step is
  actually checked) and from a =[agent-run]= step (mechanical scaffolding the
  agent still executes directly). Prose-only, like v10-v12 — not separately
  mechanically validated, since the validator has no way to judge whether a
  given =[you-run]= step's lesson is "writing code" at all, let alone whether
  a suggestion block is adequately labeled.
- *v15* (2026-09-04): Applied findings from internal design-rationale-
  capture research (IBIS/QOC/ADR lineage) to
  [[file:02-expansion-guide.org][02-expansion-guide.org]]'s External
  Session Workflow and Notes section. The session-export ask (Template's
  =For AI Assistants= boilerplate, and the External Session Workflow's own
  Workflow step 2) now requires the external AI to also report every
  decision, roadblock, or pivot hit during the session — not just
  per-step output — in a lightweight Y-Statement-style form (issue,
  options considered, choice, trade-off accepted). New =* Notes= entry
  type, =*Decision:*=, to hold these. New CRITICAL RULE — capture
  decisions live, not reconstructed afterward — requiring the entry come
  from the external AI's own export (written while it still has the
  actual conversation in context) rather than the human's memory during
  reconciliation, since hindsight reliably sanitizes real deliberation
  into a cleaner story than what happened (the research's
  post-hoc-rationalization finding). Deliberately kept to a sentence or
  two per decision rather than adopting a full argument-graph notation
  (IBIS/AIF) — the same research documents those failing to get adopted
  in practice from live-capture cognitive overhead, which is exactly the
  failure mode this system's existing plain-text, capture-after-the-fact
  design already avoids. Requested directly: the user's own experience is
  that session-export reports were losing exactly this content, reporting
  only step outputs from sessions that were "rich in decisions,
  roadblocks, questions and pivots."
- *v14* (2026-09-04): Applied three findings from internal Cognitive Load
  Theory / worked-example research to
  [[file:02-expansion-guide.org][02-expansion-guide.org]]. (1) New
  CRITICAL RULE in the Guide section: a =*Why:*= line must pass a
  load-bearing test — retained only if it states a non-obvious invariant,
  constraint, or side effect; cut if it restates the step in prose (the
  redundancy effect) or reaches for interesting-but-irrelevant color (the
  seductive-details effect). Prose-only, like v10-v12 — not separately
  mechanically validated, since the validator has no way to judge a
  =*Why:*= line's content, only its presence. (2) New optional guidance
  in the same section: group a checklist past roughly five or six steps
  under =** <Subgoal Label>= sub-headings (the subgoal-labeling finding —
  functional grouping over a flat list measurably improves transfer).
  Opt-in, not required — =SCRIPTS/validate-walkthroughs.py= already finds
  checklist steps by scanning for the checkbox pattern regardless of what
  headings sit between them, so this needed no validator change. (3) New
  step 5 in the Authoring Workflow: audit the freshly-written checklist
  for gaps before moving on, since the Cognitive Task Analysis finding
  cited in the research (experts fail to articulate roughly 70% of the
  decisions they actually make) applies directly to whoever — frequently
  an AI — just solved the problem and is now the one writing the
  checklist down. Requested directly, after the user pointed at the
  RESEARCH directory and asked to apply what it contains.
- *v13* (2026-08-25): Removed the Quiz module (=03-quiz-guide.org=) and the
  Flashcards feature (=06-flashcards-guide.org=, =.agents/flashcards/=)
  entirely, along with every cross-reference to either. This system exists
  to document *how* a problem was solved, not to teach, quiz, or drill the
  human on it afterward — the quiz-authoring/administering machinery and the
  spaced-repetition mining pipeline were both scoped for that teaching
  purpose and are out of scope now. =walkthrough.org= no longer has a
  =* Quiz= section at all (not even optionally) —
  =SCRIPTS/validate-walkthroughs.py= no longer checks for one, no longer
  enforces the v9 quiz-timing gate, and no longer tracks
  =#+LAST_REVIEWED:= staleness. =02-expansion-guide.org='s =For AI
  Assistants= boilerplate is trimmed to orientation-on-load, skepticism of
  the file's own claims, and mandatory pre-session research — Socratic-first
  prompting, interleaved per-step recall checks, and spaced-repetition
  tracking are removed as teaching mechanics, not just because they lived
  next to the Quiz. The External Session Workflow's closed-question
  self-check quiz ask is removed for the same reason; an external session
  now closes out with just a session-export summary. Requested directly.
- *v12* (2026-08-23): Strengthened the External Session Workflow's
  [[file:02-expansion-guide.org][CRITICAL RULE — external session output is
  a claim, not a record]] in =02-expansion-guide.org=: "verified" now
  explicitly means the reconciler independently re-checked the claim
  against a live, current, authoritative source — not merely that the SESH
  file contained something that looked like real evidence. The prior
  wording required real output to *exist and be pasted in*, but never
  explicitly required the reconciler to go re-fetch and confirm it. Found
  by noticing the gap directly: several reconciliation sessions this same
  day had already been doing this (re-fetching sources, catching a SESH
  claim that was accurate but incomplete, catching citations that didn't
  hold up), but nothing in the instructions actually required it — the
  practice was ahead of what was written down. Prose-only, like v10/v11 —
  not separately mechanically validated (there's no way to check whether a
  re-fetch actually happened from the file alone).
- *v11* (2026-08-23): New =For AI Assistants= pillar in
  [[file:02-expansion-guide.org][02-expansion-guide.org]] (descriptive text
  and Template boilerplate both): whatever AI loads this file is now
  required to actually run every query in =* Search Prompts= itself,
  before starting — its own independent pre-session grounding, not a
  substitute for the human's own reason to run those queries later.
  Requested directly: the v10 skepticism rule addressed an AI trusting the
  file's claims uncritically during individual confirmation steps; this
  extends the same discipline proactively, up front, using content that
  already existed in every package (Search Prompts) rather than requiring
  new research infrastructure. Prose-only, same as v10 — not separately
  mechanically validated (the validator has no way to check whether a
  search actually happened), consistent with how the rest of =For AI
  Assistants='s pedagogy is handled.
- *v10* (2026-08-23): New pedagogical pillar in
  [[file:02-expansion-guide.org][02-expansion-guide.org]]'s =For AI
  Assistants= section (both the descriptive text and the Template's actual
  boilerplate): explicit skepticism of the walkthrough file's own claims,
  not just of the human's guesses, plus a direct instruction to do real,
  independent research for confirmation steps rather than narrating
  agreement with what the file already assumes. Raised directly in
  conversation: an external AI session (Gemini) worked through a
  walkthrough file "with disregard of whether the file is correct or not"
  — it treated the Problem section and =*Why:*= lines as ground truth
  instead of things to independently verify, the same failure mode that
  produced the =44c128d0= citation error v9 already fixed at the authoring
  side. This closes the matching gap on the *reading* side: any AI loading
  this file, including one running in a different product entirely, is now
  told explicitly not to trust the file uncritically. Prose-only, like the
  rest of =For AI Assistants='s pedagogy content (v4) — not separately
  mechanically validated beyond the section's existing presence check,
  consistent with how that content has always been handled.
- *v9* (2026-08-23): New CRITICAL RULE in
  [[file:03-quiz-guide.org][03-quiz-guide.org]]: a walkthrough's Quiz must
  not be authored until its Guide checklist is fully checked (every step
  has a real =*Result:*= block) — never at expansion time. Previously the
  Quiz was written alongside the Problem and Guide sections, before any
  step was worked; since the author (frequently an AI that had already done
  the grounding research) already knew roughly where the steps would land,
  the questions and Answer Key risked quietly encoding a predetermined
  decision, turning the live Quiz into reconstructing a predicted answer
  instead of genuine retrieval practice. =SCRIPTS/validate-walkthroughs.py=
  now enforces this bidirectionally for a package declaring v9+: a
  =* Quiz= section is a hard failure if present while the checklist isn't
  fully checked, and remains required (exactly 5 questions, exactly 5
  answers, unchanged) once it is. Also adds a new required
  [[file:02-expansion-guide.org][Authoring Workflow]] section to
  =02-expansion-guide.org=, documenting the actual research-and-writing
  process for a fresh walkthrough as an explicit source of truth — asked
  for directly, after this same gap was raised in conversation — including
  a CRITICAL RULE that a citation to an earlier walkthrough or wiki page
  must be independently re-verified before being reused (found by
  discovering =index.org='s own description for issue =44c128d0= had
  mis-cited the architecture walkthrough for a claim it never actually
  made). Both new rules are version-gated, not unconditional — existing
  packages keep the ordinary version-staleness warning rather than being
  retroactively failed.
- *v8* (2026-08-22): =SCRIPTS/validate-walkthroughs.py= now checks that
  every =[[file:...]]= link in every active walkthrough, archived
  walkthrough, =index.org=, and wiki page actually resolves to a real
  file — not just the specific SESH- and Justified-By-shaped links checked
  before. Found by making the identical relative-path mistake twice in the
  same session: a wiki cross-link written from inside an active package
  directory (=.agents/walkthroughs/<uuid>/=, two levels below =.agents/=)
  used =../wikis/...= (one level) instead of =../../wikis/...= (two
  levels) — a link that reads as plausible and was never independently
  re-verified by opening it. Unconditional, like the other link-integrity
  checks — regardless of which guide version a package declares.
- *v7* (2026-08-22): =SCRIPTS/validate-walkthroughs.py= now catches a
  session-export file whose name is *close* to =NN-<tool>-SESH.<ext>= but
  missing a required piece (e.g. =01-SESH.md=, missing the =<tool>=
  segment) — previously such a file matched neither the naming check nor
  the must-be-linked check, so it sat in a package directory completely
  invisible to the validator. Found by actually hitting this exact case: a
  session export came back named =01-SESH.md= instead of
  =01-GEMINI-SESH.md=, and the validator raised nothing. Any filename
  containing "sesh" (case-insensitive) that doesn't match the strict
  pattern now fails explicitly. Unconditional, like the other SESH-related
  checks — regardless of which guide version a package declares.
- *v6* (2026-08-22): Reverses v5, below. A session-export file now stays in
  whatever format the external tool actually produced — renamed to
  =NN-<tool>-SESH.<ext>= (e.g. =01-GEMINI-SESH.md=) but never converted to
  =.org=. Converting was pure token spend with no matching benefit: the SESH
  file is a raw evidence artifact that gets reconciled *into*
  =walkthrough.org='s own =*Result:*= blocks — which do stay =.org=, since
  those live in a real package file — never validated against the
  walkthrough structure itself. =SCRIPTS/validate-walkthroughs.py='s v5
  check that failed on a non-=.org= SESH file is removed; the
  =NN-<tool>-SESH.<ext>= naming pattern and the must-be-linked-from-Notes
  rule are unchanged. Unconditional, same as the rule it replaces.
- *v5* (2026-08-21): New CRITICAL RULE in the External Session Workflow
  ([[file:02-expansion-guide.org][02-expansion-guide.org]]): most external
  AI tools can't natively emit =.org= (e.g. Gemini exports Markdown) — that
  export is fine to capture as-is, but it must be *converted* to =.org=
  during reconciliation before it counts as the package's session-export
  file, with the original-format file removed rather than kept alongside
  it. =SCRIPTS/validate-walkthroughs.py= now fails on any
  =NN-<tool>-SESH.<ext>= file whose extension isn't =.org= — previously
  such a file was invisible to the validator entirely (only the exact
  =.org= naming pattern was ever checked). This rule is unconditional,
  like the existing SESH-link-must-exist check — a package either has a
  stray non-.org export sitting in it or it doesn't, regardless of which
  guide version it declares.
- *v4* (2026-08-21): =For AI Assistants= (see
  [[file:02-expansion-guide.org][02-expansion-guide.org]]) now carries real
  pedagogy, not just behavior rules: orientation-on-load (summarize
  progress before proceeding, since a fresh AI session has no memory of
  prior ones), Socratic-first explanations (ask the human's guess before
  revealing =*Why:*=), and interleaved recall checks after each step
  instead of testing only at the terminal Quiz. Also adds a new header
  keyword, =#+LAST_REVIEWED:=, for spaced-repetition tracking — the
  validator warns (never fails) when a package's checklist is fully
  checked but =#+LAST_REVIEWED:= has gone stale (>30 days), signaling a
  refresher session is due. Section presence is still gated at v2+ per the
  existing rule; the new content simply changes what the boilerplate text
  itself says.
- *v3* (2026-08-21): New CRITICAL RULE in
  [[file:02-expansion-guide.org][02-expansion-guide.org]]'s Guide
  section — a checked (=[X]=) step must contain a non-empty =*Result:*=
  block or the package fails validation. This is the mechanical
  enforcement of "no step is marked done without its own real, verified
  output": before this version the rule was prose only (in the Teaching
  Contract and =For AI Assistants=) and a box could be checked with no
  evidence at all without the validator noticing.
- *v2* (2026-08-21): New =External Session Workflow= section in
  [[file:02-expansion-guide.org][02-expansion-guide.org]] — a
  =NN-<tool>-SESH.org= naming convention for session-export files brought
  back from working a walkthrough in a separate AI chat, plus CRITICAL
  RULEs that such exports are claims (not evidence) requiring real verified
  output to reconcile, that a closed-question self-check quiz never
  satisfies the walkthrough's own open-ended Quiz, and that every
  session-export file must be linked from =walkthrough.org='s Notes section
  — checked by the validator. Also adds a required =* For AI Assistants=
  section (fixed boilerplate, see the Template) briefing whatever AI reads
  the file next on the teaching contract and the exact session-report
  format expected back — required for packages declaring v2+; a package
  still declaring v1 only gets the existing version-staleness warning.
- *v1* (2026-08-21): =* Problem=, =* TODO ... [x/n]= Guide (checklist
  steps each carrying their own =:ID:=), =* Notes=, =* Search
  Prompts=, =* Quiz= + =** Answer Key= (exactly 5 questions, exactly 5
  answers). Baseline version — covers everything required as of the
  =#+WALKTHROUGH_GUIDE_VERSION:= line's own introduction.
